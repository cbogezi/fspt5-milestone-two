const { expect } = require("chai");
const Store = require("../src/classes/store");
const { isClass } = require("./utilities");

let store = null;

describe("Store", () => {
  beforeEach(() => {
    store = new Store();
  });
  describe("Store", () => {});
});
