const { expect } = require('chai');
const matchingParens = require('../src/matching_parens');

describe('The matchingParens function', () => {
  it('should ...', () => {

    // console.log(validParens("")); // true
    // console.log(validParens("[]")); // true
    // console.log(validParens("[](){}")); // true
    // console.log(validParens("({)}")); // false
    // console.log(validParens("{[]}")); // true
    // console.log(validParens("](")); // false


  });
});
  

